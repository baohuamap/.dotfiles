---
description: >-
  Review the changes since a fixed point (commit, branch, tag, or merge-base) along two axes — Standards and Spec. Runs both reviews in parallel sub-agents. Use when the user wants to review a branch, a PR, work-in-progress changes, or asks to "review since X".
---

Invoke the `skill` tool with name "code-review" to load the "code-review" agent skill, then follow all of its instructions precisely to fulfill this request.

$ARGUMENTS